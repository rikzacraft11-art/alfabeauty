# Environment files
.env
.env.*
!.env.example

# Logs
*.log

# OS / editor
.DS_Store
Thumbs.db
.vscode/

# Node (if a frontend is added later)
node_modules/
.next/
dist/

# Go (if backend is added later)
bin/
*.exe
*.out

# --- Evidence pack ---
# Paket A: PASS + Evidence is meant to be auditable.
# Keep generic artifacts ignored, but allow committing Paket A evidence artifacts.
artifacts/
!artifacts/paket-a/
!artifacts/paket-a/**

# Allow evidence logs inside the Paket A evidence-pack (global *.log ignore above).
!artifacts/paket-a/**/*.log

# Working docs folder (legacy) stays ignored.
docs/

# Supabase CLI local state (do not commit)
supabase/.temp/
supabase/

tmp/
reference/

# Prometheus (local ops tooling)
prometheus/data/
prometheus/secrets/lead_api_admin_token