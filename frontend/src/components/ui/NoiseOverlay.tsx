/**
 * NoiseOverlay: Subtle grain texture overlay for depth.
 * Pure CSS solution using SVG filter.
 */
export default function NoiseOverlay() {
    return (
        <div
            className="noise-overlay"
            aria-hidden="true"
        />
    );
}
