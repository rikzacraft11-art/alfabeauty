# Example env file (safe to commit)

# --- App ---
APP_ENV=development
PORT=8080

# --- Database ---
# Local example (docker-compose):
# DATABASE_URL=postgres://app:app@localhost:5432/app?sslmode=disable
DATABASE_URL=postgres://app:app@localhost:5432/app?sslmode=disable
TRUSTED_PROXIES=

# --- Lead API (Option B) ---
LEAD_API_ADMIN_TOKEN=__CHANGE_ME__
LEAD_API_RATE_LIMIT_RPS=5
LEAD_API_MAX_BODY_BYTES=65536

# --- Analytics / RUM ---
ANALYTICS_ENDPOINT_URL=https://example.com/collect

# --- Error logging (optional) ---
SENTRY_DSN=

# --- Lead notifications (optional) ---
# Enable/disable channels:
LEAD_NOTIFY_EMAIL_ENABLED=false
LEAD_NOTIFY_WEBHOOK_ENABLED=false

# SMTP (required when LEAD_NOTIFY_EMAIL_ENABLED=true)
SMTP_HOST=
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM=no-reply@example.com
# Comma-separated list of recipients
SMTP_TO=ops@example.com
SMTP_USE_TLS=true

# Webhook (required when LEAD_NOTIFY_WEBHOOK_ENABLED=true)
# For Google Sheets integration, point this to your Apps Script / webhook URL.
GOOGLE_SHEETS_WEBHOOK_URL=
GOOGLE_SHEETS_WEBHOOK_SECRET=
